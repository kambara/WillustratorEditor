class util.Point extends flash.geom.Point {
	//
	// オーバーライドはしない
	//
	/*
	public function Point(x:Number, y:Number) {
		this.x = x || 0;
		this.y = y || 0;
	}
	
	public function abs():Point {
		return new Point(Math.abs(x), Math.abs(y));
	}
	*/
	/*
	public function reverse():Point {
		return new Point(-x, -y);
	}
	public function toString():String {
		return (x.toString()+","+y.toString());
	}
	
	public function minus(p:Point):Point {
		return new Point(x - p.x, y - p.y);
	}
	
	public function abs():Point {
		return new Point(Math.abs(x), Math.abs(y));
	}
	public function reverse():Point {
		return new Point(-x, -y);
	}
	
	//public var x:Number;
	//public var y:Number;
	
	function Point(x:Number, y:Number) {
		this.x = x || 0;
		this.y = y || 0;
	}
	*/
	/*
	public function clone():Point {
		return new Point(x, y);
	}
	public function equal(p:Point):Boolean {
		if (this.x == p.x && this.y == p.y) {
			return true;
		} else {
			return false;
		}
	}
	public function plus(p:Point):Point {
		return new Point(x + p.x, y + p.y);
	}
	*/
}